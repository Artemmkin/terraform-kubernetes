## How to connect to the cluster with kubectl
Run the command from terraform output in the form of:

```bash
gcloud container clusters get-credentials my-cluster --zone europe-west1-b --project example-123456
```
